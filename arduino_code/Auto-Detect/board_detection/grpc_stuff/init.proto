syntax = "proto3";

service FetchCredentials {
  rpc SendArduinos (Arduinos) returns (ChoiceAndCredentials);
}

message ChoiceAndCredentials{
  int32 Choice = 1;
  string SSID = 2;
  string Password = 3;
}

message Arduinos{
  repeated string Arduino = 1;
}